[user]
    email = thomas.perale@openmailbox.org
    name = Perale Thomas

[push]
    default = current

[color]
    ui = true

[credential]
    helper = cache

[alias]
    ignore = "!gi() { curl -L -s https://www.gitignore.io/api/$@ ;}; gi"
[core]
    editor = "vim"
    pager = diff-so-fancy | less --tabs=4 -RFX
